# Active Context

Current focus: Post-TASK045 — README Star History chart added.

Completed build slices:
- Mobile M1: build wiring stable.
- Backend B1: bootstrap, Flyway, actuator health UP.
- Backend B2: JWT auth (register/login/refresh), Spring Security filter chain, alg:none rejected, BCrypt passwords.
- Backend B3: Track JPA entity + repository, GET /api/tracks paginated, GET /api/tracks/{id}.
- Mobile M2: Retrofit + OkHttp DI, AuthApiService, AuthRepository, AuthViewModel, LoginScreen, RegisterScreen, AppNavigation with auth guard.
- Mobile M3: ExoPlayer Hilt module, PlayerController (StateFlow playback state), SerenadePlayerService (MediaSessionService), manifest service + foreground perms.
- Backend B4: MinioConfig + MinioService, RabbitConfig (exchange/queue/binding), TrackUploadService, POST /api/tracks/upload.
- Mobile M4: TrackApiService, TrackSyncRepository, TrackListViewModel, TrackListScreen (LazyColumn + PullToRefreshBox), MiniPlayerBar, AppNavigation Scaffold.
- Backend B5: POST /internal/tracks/{id}/ready (X-Api-Key, marks READY with streamUrl+durationMs), POST /internal/tracks/{id}/failed. Python transcoder worker: aio-pika consumer → Pydantic message validation → MinIO download → FFmpeg HLS transcode (shell=False, explicit args) → MinIO upload hls/{trackId}/ → SpringClient callback to backend.
- Mobile M5: PlayerController.syncPosition() for clean position polling, PlayerViewModel (500ms poll loop), PlayerScreen (artwork placeholder, Slider seek bar, play/pause, skip stubs), ROUTE_PLAYER in nav graph, mini player bar tap opens full-screen.
- Backend B6: SubtitleLine JPA entity (subtitle_lines table already in V1__init.sql), SubtitleLineRepository, GET /api/tracks/{id}/subtitles (JWT), POST /internal/tracks/{id}/subtitles (X-Api-Key bulk push), subtitler queue + binding added to RabbitConfig.
- Workers B6: subtitler worker — aio-pika consumer listens serenade.subtitler (same track.uploaded event as transcoder, runs parallel), Pydantic message validation, raw object key derived from validated UUID trackId, MinIO download raw → faster-whisper → deterministic uuid5 segment IDs → SpringClient callback. WHISPER_MODEL env var added to shared settings; WORKER_API_KEY required.
- Mobile M6: SubtitleApiService (Retrofit), SubtitleRepository (in-memory, no Room cache), PlayerViewModel extended with _subtitleLines MutableStateFlow + currentCue combine StateFlow, PlayerScreen AnimatedVisibility cue text between seek bar and controls.
- Backend B7: SearchController GET /api/search, native query on TrackRepository (plainto_tsquery + ts_rank + genre filter), no migration needed (search_vector + GIN index in V1).
- Mobile M7: SearchApiService (reuses TrackResponse/PageResponse), SearchViewModel (debounce + flatMapLatest + genre toggle), SearchScreen (inline TextField in TopAppBar + FilterChips + LazyColumn), ROUTE_SEARCH in AppNavigation, Search icon in TrackListScreen TopAppBar, toEntity() extension for playback on result tap.
- Mobile build stabilization: added missing Retrofit/Hilt providers for PlaylistApiService and RatingApiService in NetworkModule.
- Backend playlists/ratings: Playlist CRUD, owner-only detail/copy/update/delete, playlist track replacement with duplicate track/position guard, copy creates "`Name Copy`", rating upsert validates target existence, blocks rating own uploaded tracks, caches averages in Redis on a best-effort basis.
- Mobile playlists/ratings: LibraryScreen lists playlists and creates new playlists; PlaylistDetailScreen shows tracks, average rating, 1-5 star rating, copy action, and track playback from playlist detail.
- Mobile downloads/offline: DownloadRepository queues expedited WorkManager downloads, Hilt DownloadWorker saves stream URL to app-private storage, Room download progress/state updated, tracks marked downloaded with localPath, TrackSyncRepository preserves localPath/isDownloaded on refresh, TrackListScreen has download/delete actions, DownloadScreen lists downloaded tracks, AppNavigation plays local file URI before stream URL, completion notification posted when permission allows.
- Backend sync: GET /api/changes?since=&limit= returns updated READY tracks, owned playlists, user ratings, and nextCursor.
- Mobile sync: ChangesApiService + SyncRepository + Hilt SyncWorker pull changes, upsert tracks/playlists/ratings into Room, store cursor in private SharedPreferences, schedule periodic 15-minute network-connected sync from TrackListViewModel, pull changes during refresh, and drain pending playlist/rating outbox ops.
- Mobile outbox: PlaylistRepository writes local Room rows and PendingOpEntity for create/copy, then attempts remote sync; RatingRepository writes local rating and PendingOpEntity for track/playlist ratings. SyncRepository flushes all op types including ADD/REMOVE_PLAYLIST_TRACK.
- Security hardening: Android debug OkHttp logging redacts Authorization; backend application.yaml no longer contains JWT/internal API-key defaults; SecurityConfig fails fast if INTERNAL_API_KEY is missing. Backend .env-example now documents INTERNAL_API_KEY and WORKER_API_KEY.
- Backend security headers: SecurityConfig adds X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: no-referrer, Content-Security-Policy: default-src 'none'.
- Backend upload status: GET /api/uploads/{trackId} returns authenticated uploader-scoped processing state.
- Mobile M7 upload: UploadScreen uses SAF OpenDocument, metadata form, genre chips, multipart Retrofit upload with OkHttp progress RequestBody, and polling until READY/FAILED.
- Playlist track add/remove: PlaylistDetailScreen has per-row delete button and FAB that opens ModalBottomSheet picker.
- Workers admin API: shared/admin_api.py creates FastAPI app per worker with GET /health, GET /metrics, POST /admin/reprocess/{track_id} (X-Api-Key). Both workers run uvicorn alongside aio-pika consumer.
- Backend B8: RateLimitFilter — bucket4j in-memory per-IP rate limiter: 10 RPM for /api/auth/**, 60 RPM for all other /api/** endpoints. Returns HTTP 429 with JSON error on breach. RabbitConfig extended with DLQ: serenade.dead-letter DirectExchange + serenade.transcoder.dlq and serenade.subtitler.dlq queues.
- Mobile M8: PlaylistDetailViewModel myRating bug fixed — rating now loaded from RatingDao.getByTargetOnce on init/reload. Playlist drag-reorder implemented: moveTrackUp/moveTrackDown update state optimistically + write REORDER_PLAYLIST_TRACKS PendingOp + call setTracks. PlaylistDetailScreen shows up/down arrow buttons per track row. Offline upload outbox: UploadRepository.isNetworkAvailable() + queueUpload() copies file to app-private pending_uploads/ dir and writes UPLOAD_TRACK PendingOp. UploadViewModel falls back to queue when offline, shows QUEUED status. SyncRepository handles REORDER_PLAYLIST_TRACKS and UPLOAD_TRACK ops; else branch changed from throw to Unit. RatingDao.getByTargetOnce added.
- Backend B9/DLQ hardening: RabbitConfig main worker queues declare x-dead-letter-exchange/routing keys; transcoder/subtitler use `message.process(requeue=False)` for processing failures; RateLimitFilter has login 5/min, register 3/hour, auth 10/min, API 60/min policies with `Retry-After`; `X-Forwarded-For` is trusted only from configured proxy IPs; CORS uses YAML allow-list; BCrypt strength is 12; HSTS is enabled; Swagger/OpenAPI requires auth; MinIO presigned expiry clamps to 1-15 minutes.
- Backend B10 observability: RequestIdFilter accepts/sanitizes `X-Request-Id`, generates one when absent, writes MDC, returns the header, and logs method/path/status/duration. Upload RabbitMQ messages carry `X-Request-Id`; transcoder/subtitler read request IDs from message headers, include them in JSON logs, and forward them on SpringClient callbacks. Spring Boot console logging uses logstash JSON. Actuator exposes `metrics` and `prometheus` behind normal auth with Micrometer app tags and HTTP server histograms.
- Mobile offline/security hardening: AndroidManifest no longer enables app-wide cleartext or uses a stale `tools:replace`; network_security_config now limits cleartext to emulator/local/LAN dev hosts and removes the production domain; backup_rules and data_extraction_rules explicitly exclude all app data; DownloadWorker detects `.m3u8` URLs, downloads the manifest plus media segments into app-private storage, rewrites segment references to local files, stores the local manifest path, and cleans partial files on failure.
- Mobile queue playback: PlayerController now accepts `PlaybackItem` queues, resolves relative/file/content URLs safely, sets HLS MIME only for `.m3u8`, exposes queue index/size and next/previous availability, and supports skipToNext/skipToPrevious. PlayerScreen enables skip controls and shows queue position. AppNavigation tracks the active queue metadata so mini/full player titles follow Media3 item transitions. Search and playlist detail taps enqueue the visible result/playlist list instead of only a single track. PlayerController no longer logs stream URLs.
- Mobile persisted playback: Room now has `playback_queue` and `playback_history` tables with DAO/repository wiring. PlayerController saves queues when playback starts, restores the latest queue on app startup without autoplay, persists current queue position/progress, and records recently played tracks when playback advances. Mini/full player fall back to restored Media3 metadata when navigation-local queue state is empty.
- Mobile connectivity fix: app `BuildConfig.API_BASE_URL` no longer hardcodes a single LAN IP. `app/app/build.gradle.kts` now reads `SERENADE_API_BASE_URL` from Gradle property or environment variable and defaults to `http://10.0.2.2:8080/` for Android emulator. This keeps search, Retrofit-backed downloads, and Media3 `/hls/**` playback pointed at the reachable backend by default.
- Mobile page decode fix: `PageResponse` now defaults `totalElements`, `totalPages`, and `last` when the backend page serializer omits those root fields. Search, track list sync, and playlist list calls can consume Spring page responses where `content` is present but metadata shape differs.
- Mobile playback keepalive fix: PlayerController starts `SerenadePlayerService` before play/resume/skip so playback is tied to a media playback foreground service. PlayerModule configures Media3 with `C.WAKE_MODE_NETWORK`, media audio attributes, audio focus, and noisy-device handling. SerenadePlayerService now releases only the MediaSession on destroy, not the app-scoped singleton ExoPlayer.

Latest persisted playback fix: playback queue and recently played history are now stored in Room. Restored queues use saved Media3 metadata and resume at the persisted item/position without autoplay. Follow-up crash fix: migration 1->2 now recreates the new playback tables and creates `updatedAt`/`lastPlayedAt` as `INTEGER` epoch millis to match `AppConverters.fromInstant()` and Room's expected schema.
Latest collaboration docs fix: `.gitignore` now allows shared root markdown files, `memory-bank/**/*.md`, `.github/instructions/**/*.md`, and `.claude/commands`/`.claude/skills` markdown files so teammates receive project state and agent instructions. `.claude/settings.local.json` remains ignored as local machine state.
Latest HLS seekability fix: Backend `GET /hls/{trackId}/index.m3u8` now normalizes served manifests as VOD by injecting `#EXT-X-PLAYLIST-TYPE:VOD` and `#EXT-X-ENDLIST` when missing, returning exact `Content-Length` for the normalized bytes. This helps existing uploaded HLS tracks become seekable without re-transcode. Transcoder now generates audio-only VOD HLS by mapping `0:a:0`, disabling video/artwork streams, and passing `-hls_playlist_type vod`.
Latest playback service fix: `SerenadePlayerService` now calls `ensureForeground()` in `onCreate()` before Media3 notification/session setup, so a `startForegroundService()` launched from track tap satisfies Android's foreground-service deadline even if later Media3 setup or playback startup takes time.
Latest playback notification fix: SerenadePlayerService still calls `startForeground()` immediately after `startForegroundService()` to satisfy Android's foreground-service timeout, but the anchor notification now uses the music notification icon, Media3 media style, and current track metadata instead of the launcher icon/app-name placeholder. Playback queue items include title, artist, album, duration, and artwork URL metadata for notification artwork and timeline duration. The service listens to Media3 player events and refreshes both the visible foreground notification and Media3 provider notification with `triggerNotificationUpdate()` on item/metadata/playback/timeline changes, so title/artist/timeline refresh when the user presses next/previous in system controls.
Latest Serenade handoff design update: `serenade-handoff.zip` README and `serenade/project/Serenade.html` were read with imported JSX screens/tokens. Existing Android Compose screens were reskinned instead of adding new pages: shared `SerenadeDesignSystem` gained prototype-aligned screen background/card/artwork treatment; Search, Library, Upload/Studio, Player, MiniPlayer, and bottom navigation visuals now follow the handoff palette, cards, chips, and artwork motif. No new routes or behavior were added without confirmation. Verified with `./gradlew --gradle-user-home ../.gradle-user-home :app:compileDebugKotlin` and `git diff --check`.
Latest splash/You/theme/logo update: Requested `serenade.zip` was not present in the workspace; only `serenade-handoff.zip` was available and used as the design source. Added a Compose `SplashScreen` route before auth/home routing, a new `YouScreen` bottom tab, DataStore-backed `ThemePreferenceStore`, persisted Midnight Velvet / Aurora Pulse selector, dynamic app palette wiring through `AppTheme`, logout/downloads entries on You, and Serenade launcher logo vector resources wired in `AndroidManifest.xml`. Verified `./gradlew --gradle-user-home ../.gradle-user-home :app:compileDebugKotlin` and `git diff --check`.
Latest Upload/Downloads design update: Existing `UploadScreen` now better matches the Serenade Studio prototype with a send top action, editorial header, raised audio/artwork cards, themed form fields/chips, and status/progress card treatment. Existing `DownloadScreen` was rebuilt around the offline design with a storage/cache card, empty state, offline track cards, artwork motif, ready badges, duration metadata, and delete actions. Behavior remains scoped to existing completed downloads; no new queue/progress state was added. `serenade.zip` appeared after implementation and was read; its relevant Upload/Downloads snippets matched the handoff source used. Verified `./gradlew --gradle-user-home ../.gradle-user-home :app:compileDebugKotlin` and `git diff --check`.
Latest email auth update: Backend now has Spring Mail + Thymeleaf email templates and MailHog dev service/config. `register` creates an unverified user and emails a hash-stored unique 5 digit code expiring in 10 minutes; `verify-email` consumes it, marks email verified, sends welcome email, and returns JWTs. `resend-verification` manually refreshes verification, login auto-refreshes when an unverified user has no active code, and forgot/reset password use the same code lifecycle. Android auth now supports details -> 5 digit verify signup, unverified-login verify fallback, and forgot/reset password screens. Biometric and terms checkbox were intentionally not added. Debug OkHttp logging is BASIC to avoid logging passwords/codes. Verified `sh mvnw -DskipTests package` and `./gradlew :app:compileDebugKotlin`.
Latest You/Flyway fix: `V2__auth_email_codes.sql` was restored to its originally applied Flyway checksum and `V3__relax_auth_code_hash_index.sql` now carries the follow-up auth-code index relaxation. You screen no longer forces scrolling on fitted content, removing the visible right-edge scroll thumb; `SrScreenBackground` uses finite `Offset(1200f, 0f)` instead of `Float.MAX_VALUE` to avoid gradient edge artifacts. Verified `sh mvnw -DskipTests package`, `./gradlew :app:compileDebugKotlin`, and `git diff --check`. Sandbox `spring-boot:run` could not connect to local Postgres because socket creation is blocked.
Latest playback lifecycle fix: Logout stops playback before clearing auth state, clears current/persisted playback queues, and resets mini-player state. `SerenadePlayerService` handles task removal by stopping ExoPlayer, clearing media items, removing foreground notification, and stopping itself. `MainActivity` stops playback when explicitly finishing. Verified `./gradlew :app:compileDebugKotlin`.
Latest theme preview fix: You screen theme options now build preview swatches from `colorsFor(choice)` instead of current live palette values. This keeps Midnight Velvet and Aurora Pulse previews distinct after either theme is selected. Verified `./gradlew :app:compileDebugKotlin`.
Latest signup setup polish: `DigitCodeField` provides five separated numeric boxes for verification/reset code entry. Register verification, unverified-login verification, and reset password use it. Signup now has step 3/3 after email verification with an optional persisted theme picker; Skip or Tune & enter moves to home. Verified `./gradlew :app:compileDebugKotlin`.
Latest home polish: Listen header avatar now uses saved user display name/username first initial with `U` fallback instead of hardcoded `J`. The Good evening header height is reduced to `150.dp`; its right radial gradient uses a finite center. Mini-player progress bar is lowered closer to the bottom card edge. Verified `./gradlew :app:compileDebugKotlin`.
Latest README update: README now includes a live Star History chart for `ACHRAF-YOUSSEF/serenade` using a dark/light `<picture>` embed from Star History.
Known connectivity limitation: physical devices still need `SERENADE_API_BASE_URL=http://<host-lan-ip>:8080/` when building/installing the app.
Known artwork limitation: artwork presigned URLs expire in ≤15 min; app re-fetches fresh URLs on next sync. Coil caches in memory/disk so expiry only affects cold loads.
Latest artwork fix: UI should not use stored MinIO presigned artwork URLs directly. Backend exposes stable `GET /artwork/{trackId}` and Android normalizes nonblank artwork markers to `${BuildConfig.API_BASE_URL}/artwork/{trackId}` in `stableArtworkUrl(...)`. This lets Coil load uploaded artwork from the same backend host used by the phone/emulator and avoids expired/direct MinIO URLs.
Known DLQ limitation: existing RabbitMQ queues need delete + redeclare to pick up new x-dead-letter-exchange argument.
Known observability limitation: request tracing is request-id based only; no distributed tracing backend/OpenTelemetry exporter is wired yet.
Known rate limiter note: Redis sliding window uses fixed windows (INCR+PEXPIRE); not a token bucket. Suitable for single-instance deployments; upgrade to bucket4j distributed ProxyManager for multi-instance.

Next build slice: FCM push notification for upload/transcode status; search result artwork thumbnails; OpenTelemetry exporter wiring.

Read before work:
- root progress.md
- memory-bank/tasks/_index.md
